<div class="container">
  <%= simple_form_for [@restaurant, @restaurant.images.new] do |f| %>
    <div class="form-group">
      <%= f.input :cloudinary_url %>
      <%= f.input :cloudinary_url_cache, as: :hidden %>
    </div>
    <%= f.submit "Add an image" %>
  <% end %>
</div>
